version: '3.8'

services:
  arena_champion_bot:
    build: .
    container_name: arena_champion_bot
    restart: always
    environment:
      - TZ=UTC
      - OLLAMA_HOST=http://host.docker.internal:11434
      - AGENT_TOKEN=ak_Y03Iz3W2slXypgabOqftqSsWINwXfVzw   # <--- Вставьте сюда ваш токен
    volumes:
      - ./data:/app/data